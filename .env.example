# Recommended: native DeepSeek V4 Pro integration
MODEL_PROVIDER=deepseek
DEEPSEEK_API_KEY=your-deepseek-api-key
DEEPSEEK_MODEL=deepseek-v4-pro
DEEPSEEK_API_BASE=https://api.deepseek.com
DEEPSEEK_THINKING=true
DEEPSEEK_REASONING_EFFORT=high

# Optional: OpenAI or another OpenAI-compatible service
OPENAI_API_KEY=your-api-key
OPENAI_MODEL=gpt-4.1-mini
# Leave empty for OpenAI. Example:
# Local Ollama: http://localhost:11434/v1 (use any non-empty API key)
OPENAI_BASE_URL=

# Agent controls
AGENT_TEMPERATURE=0
AGENT_MAX_ITERATIONS=25
AGENT_MAX_PLAN_STEPS=8
AGENT_TIMEOUT_SECONDS=120
DATA_AGENT_RUNS_DIR=./runs

# LangSmith observability and deployment
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=your-langsmith-api-key
LANGSMITH_PROJECT=data-analysis-agent
# Required only when one API key can access multiple workspaces
LANGSMITH_WORKSPACE_ID=

# Local React development server. Production uses the same deployment origin.
DATA_AGENT_CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
